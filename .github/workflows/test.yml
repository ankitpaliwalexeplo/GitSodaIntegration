name: Scan for data quality

on: pull_request
jobs:
  soda_scan:
    runs-on: ubuntu-latest
    name: Run Soda Scan
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Perform Soda Scan
        uses: sodadata/soda-github-action@v1.0.2
        env:
          SODA_CLOUD_API_KEY: 25fbe214-29bb-4e9a-9041-3a5bc5251836
          SODA_CLOUD_API_SECRET: 3lo_uwA4-aXFts7n8rsbF5mhPh2XUWT7ArxxZeIZbFdv6EmBm38-dg
          SNOWFLAKE_USERNAME: ANKITPALIWAL
          SNOWFLAKE_PASSWORD: @Nki1234
        with:
          soda_library_version: v1.0.4
          data_source: datademo
          configuration: ./configuration.yml
          checks: ./checks.yml
